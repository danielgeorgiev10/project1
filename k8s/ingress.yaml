apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: project-app-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: project-app.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: project-app-svc
            port:
              number: 80